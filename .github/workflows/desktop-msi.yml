# This GitHub Actions workflow has been disabled.
# You can safely delete this file (and the .github/ folder) from the file tree.
# The Windows installer is now built locally via:
#   artifacts/point-cloud-viewer/src-tauri/build-windows.ps1
